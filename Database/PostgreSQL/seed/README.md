# PostgreSQL Seed Data

Optional local development seed data (dataset definitions, sample schedules).
Not applied automatically. Add `*.sql` files here and load them after
migrations when a developer wants pre-populated operational state.

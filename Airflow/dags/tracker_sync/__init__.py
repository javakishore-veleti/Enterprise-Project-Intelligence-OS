"""Tracker-agnostic repository sync (connector ABC + FakeConnector + sync engine).

See ``dag.py`` for the ``tracker_repository_sync`` Airflow DAG and ``sync_engine``
for the pure, infra-free sync core.
"""
